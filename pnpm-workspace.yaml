packages:
  - "apps/*"
  - "packages/*"

# Keep host and CI installs equivalent; pnpm otherwise changes this default
# with CI detection and may attempt an implicit reinstall before a script.
enableGlobalVirtualStore: false

allowBuilds:
  "@bufbuild/buf": true
  # Published SDKs are prebuilt. Google's preinstall is a no-op; protobufjs
  # postinstall only prints a version-scheme warning. Neither is needed here.
  "@google/genai": false
  protobufjs: false

# Do not admit packages published within the last 24 hours. This slows down
# dependency-confusion and compromised-release propagation without weakening
# exact lockfile pinning.
minimumReleaseAge: 1440

# Native optional packages are materialized only for the machine running the
# workspace. The lockfile still records all supported targets for CI portability.
supportedArchitectures:
  os:
    - current
  cpu:
    - current
  libc:
    - current
